{
  "name": "progress-tracker",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "api": "poetry run uvicorn backend.main:app --host 0.0.0.0 --port 8000",
    "build": "vite build",
    "prod": "vite build && poetry run uvicorn backend.main:app --host 0.0.0.0 --port 8000",
    "preview": "vite preview",
    "test": "vitest run"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.4.1",
    "vite": "^5.4.11",
    "vitest": "^2.1.8"
  }
}